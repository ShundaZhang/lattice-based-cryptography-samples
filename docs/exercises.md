# 习题

## 1. 格点计算

给定基向量 `b1=(2,1)`、`b2=(1,3)`，计算系数 `(z1,z2)=(3,-1)` 对应的格向量。

## 2. 行列式

二维格基 `B=[(4,1),(1,3)]` 的 determinant 是多少？它的几何意义是什么？

## 3. 短向量枚举

对基 `[(4,1),(1,3)]`，枚举 `z1,z2 in {-2,-1,0,1,2}`，找出最短非零向量之一。

## 4. 好基和坏基

解释为什么同一个格可以有不同质量的基。举一个二维例子。

## 5. LLL 条件

LLL 中的 size reduction 想让 `mu[i][j]` 的绝对值大致不超过多少？这一步的直觉是什么？

## 6. LWE 无噪声情形

如果 LWE 样本中 `e_i=0`，攻击者面对的是什么问题？为什么这不安全？

## 7. Toy LWE 正确性

Toy LWE 加密中：

```text
u = A^T r
v = b^T r + bit * floor(q/2)
b = A s + e
```

证明解密时 `v - <s,u>` 等于消息编码加上小噪声。

## 8. 参数失败

为什么噪声太大时 LWE 解密会失败？用 `q=97` 时的阈值直觉解释。

## 9. 暴力攻击复杂度

Tiny LWE 中若 secret 长度为 `n`，每个坐标在 `Z_q` 中，暴力搜索 secret 的候选数量是多少？当 `n=4, q=97` 时大约是多少？

## 10. SIS 与签名

为什么 SIS 中“找短解”可以用于签名或承诺？给出高层直觉即可。

## 11. Ring-LWE 效率

Ring-LWE 为什么通常比普通 LWE 更节省公钥大小？

## 12. Module-LWE 折中

Module-LWE 相比 Ring-LWE 和普通 LWE 分别有什么折中？

## 13. KEM 和 PKE

KEM 与传统公钥加密 PKE 的接口有什么不同？为什么 TLS 这类协议喜欢用 KEM？

## 14. ML-KEM 和 ML-DSA

ML-KEM 和 ML-DSA 分别解决什么密码任务？它们为什么都属于 module-lattice 风格？

## 15. FHE 噪声增长

为什么全同态加密里要追踪噪声？bootstrapping 解决的是什么问题？

